## OpenSpec change

- Change ID: <!-- openspec/changes/<id>, or "none" for chore/docs-only PRs -->
- [ ] `openspec validate "<id>" --strict` passes
- [ ] `tasks.md` fully checked off (list any deliberately deferred tasks below)
- [ ] Delta specs synced to `openspec/specs/` and change archived (specs and code merge together)

## What & why

<!-- 1-3 sentences; link the proposal instead of restating it -->

## Verification

- [ ] `npm test` — all pass
- [ ] `npm run opsx:validate` passes
- [ ] `npm run opsx:check` — no complete-but-unarchived change
